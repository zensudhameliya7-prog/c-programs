#include <stdio.h>

int main()
{
   /*int i = 0;

    while (i < 5)
    {
        printf("%d ", i++);
    }

    return 0;*/
   // int m;
    //m//=sizeof(45l);
    //printf("%d",m);
    //return 0;
    #include <stdio.h>
#include <math.h>
int main()
{
   float a,c;
   scanf(“%f %f”,&a,&c);
   printf(“%d %d”,(int)round(a),(int)round(a));
}
   return 0;
}